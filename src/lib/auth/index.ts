/**
 * Auth module placeholder.
 * Enable with: features.auth = true in config/features.config.ts
 *
 * When implementing:
 * 1. pnpm add next-auth@beta @auth/drizzle-adapter
 * 2. Create auth.config.ts with providers (GitHub, Google, Credentials)
 * 3. Create src/app/(auth)/login/page.tsx and register/page.tsx
 * 4. Add AUTH_SECRET to .env.local
 * 5. Wire up middleware for protected routes
 *
 * See: https://authjs.dev/getting-started/installation?framework=next.js
 */

export const AUTH_READY = false;
